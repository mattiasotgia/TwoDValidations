# CUP: CAFAna Unified Plotter

## Install 

To install run the following command in this directory

```bash
pip install -e .
```

This will install the `cup` python package and the `cli` interface command `cup`.